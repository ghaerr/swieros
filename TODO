cycle count incorrect or overflow, check IDLE instruction?

function args always 8, fix to 4 or 8
calling sequence, bp frame or not
instruction set names, see com compiler
	emulator instruction profiler

em -f use mmap for ramfs

why v ^ p vs v | p in emulator?
